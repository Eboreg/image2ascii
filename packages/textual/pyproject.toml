[project]
name = "image2ascii-textual"
version = "0.1.0"
description = ""
readme = "README.md"
authors = [
    { name = "Robert Huselius", email = "robert@huseli.us" }
]
requires-python = ">=3.13"
dependencies = [
    "image2ascii",
    "textual>=8.2.4",
]

[build-system]
requires = ["uv_build>=0.9.7,<0.10.0"]
build-backend = "uv_build"

[tool.uv.sources]
image2ascii = { workspace = true }
